"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyToken = void 0;
const crypt = __importStar(require("../../lib/crypto-js"));
const constants_1 = require("../constants");
const verifyToken = (token, expired = constants_1.DEFAULT_EXPIRATION) => {
    const decryptedTimestamp = parseInt(crypt.AES.decrypt(token, constants_1.SECRET).toString(crypt.enc.Utf8) || '');
    const time = Date.now();
    const expiration = decryptedTimestamp + expired;
    if (expiration > time) {
        return true;
    }
    else {
        return false;
    }
};
exports.verifyToken = verifyToken;
